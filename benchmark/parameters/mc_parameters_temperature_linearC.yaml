mc_parameters1:
        # number of moves to perform search
        n_moves: 10000
        # if current_temperature is 0, then initial temperature is estimated
        current_temperature: 0
        # if kirkpatrick is True, we use the temp schedule c_k+1 = alpha * c_k
        temperature_linearC: True
        rho: 0.01
        # c_k+1 = alpha * c_k
        operation_schedule:
                move: 1                 
mc_parameters2:
        # number of moves to perform search
        n_moves: 10000
        # if current_temperature is 0, then initial temperature is estimated
        current_temperature: 0
        # if kirkpatrick is True, we use the temp schedule c_k+1 = alpha * c_k
        temperature_linearC: True
        rho: 0.1
        # c_k+1 = alpha * c_k
        operation_schedule:
                move: 1                 
mc_parameters3:
        # number of moves to perform search
        n_moves: 10000
        # if current_temperature is 0, then initial temperature is estimated
        current_temperature: 0
        # if kirkpatrick is True, we use the temp schedule c_k+1 = alpha * c_k
        temperature_linearC: True
        rho: 1
        # c_k+1 = alpha * c_k
        operation_schedule:
                move: 1                 
